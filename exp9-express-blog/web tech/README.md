# Writerly

A simple Express blog platform where users can create, edit, delete, and browse posts.

## Run locally

```bash
npm install
npm run dev
```

The app starts on `http://localhost:3000`.

## Features

- Create new posts
- Edit existing posts
- Delete posts
- View all posts and individual post pages
- File-backed persistence in `data/posts.json`
